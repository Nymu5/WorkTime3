<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controller="clr-namespace:WorkTime3.Controller"
             x:Class="WorkTime3.MainPage">
    <ContentPage.BindingContext>
        <controller:MainController />
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Image
                Source="dotnet_bot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <Label
                Text="{Binding Txt}"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center"/>

            <Label
                Text="Welcome to .NET Multi-platform App UI"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="Center" />

            <Button
                x:Name="CounterBtn"
                Text="Click me"
                SemanticProperties.Hint="Counts the number of times you click"
                Command="{Binding TestCommand}"
                HorizontalOptions="Center" />

            <Entry HorizontalOptions="Fill" Text="{Binding Txt}" />
            <Grid BackgroundColor="Blue" Margin="10,0,10,0">
                <StackLayout Orientation="Vertical" BackgroundColor="Red" Margin="0,2,0,2">
                    <Label Text="Name" FontAttributes="Bold" FontSize="Header" />
                    <Label Text="Id" />
                </StackLayout>
            </Grid>
            
            

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>