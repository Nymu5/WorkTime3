<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controller="clr-namespace:MyTime.Controller"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MyTime.View.AddEmployerPage"
             xmlns:controls="clr-namespace:MyTime.Resources.Templates"
             Title="Add Employer">
    <ContentPage.BindingContext>
        <controller:AddEmployerController />
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:IsStringNotNullOrEmptyConverter x:Key="NullOrEmptyConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Command="{Binding SaveEmployerCommand}" CommandParameter="{Binding Employer.Name, Converter={StaticResource NullOrEmptyConverter}}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid Background="{AppThemeBinding Dark={StaticResource Gray900}, Light={StaticResource BackgroundLight}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Text="{Binding Employer.Name, TargetNullValue='New Employer'}" FontFamily="" FontSize="Title"
                   Margin="20,50" Grid.Row="0" HorizontalTextAlignment="Center"/>
            <StackLayout Orientation="Vertical" Grid.Row="1">
                <Border BackgroundColor="{AppThemeBinding Dark={StaticResource Gray600}, Light={StaticResource White}}"
                        StrokeShape="RoundRectangle 20,20,20,20"
                        Margin="20,10,20,10" Padding="10">
                    <Border.Shadow>
                        <Shadow Brush="{AppThemeBinding Dark={StaticResource Black}, Light={StaticResource ShadowLight}}"
                                Offset="0,30" Radius="40" Opacity="0.8" />
                    </Border.Shadow>
                    <Grid>
                        <StackLayout Orientation="Vertical" Margin="10,10,10,10">
                            <Label Text="Name" FontFamily="" FontAttributes="Bold" FontSize="Header" x:Name="EmployerName">
                            </Label>
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.Name}" />
                            <Label Text="Employer #" FontFamily="" FontAttributes="Bold" FontSize="Header" />
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.EmployerNb}" />
                            <Label Text="Salary" FontFamily="" FontAttributes="Bold" FontSize="Header" />
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.Salary}" Keyboard="Numeric" />
                            <Label Text="Address Line 1" FontFamily="" FontAttributes="Bold" FontSize="Header" />
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.AddressLine1}" />
                            <Label Text="Address Line 2" FontFamily="" FontAttributes="Bold" FontSize="Header" />
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.AddressLine2}" />
                            <Label Text="Description" FontFamily="" FontAttributes="Bold" FontSize="Header" />
                            <Entry Margin="0, 5, 0, 10" Text="{Binding Employer.Description}" />
                        </StackLayout>
                    </Grid>
                </Border>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>